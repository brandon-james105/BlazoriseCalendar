@page "/"

<Container Flex="Flex.JustifyContent.Center.AlignItems.Center" Class="vw-100 vh-100">
    <Calendar @bind-Date="Date" SelectionMode="CalendarSelectionMode.Multiple" />
</Container>

@code {
    public DateTime? Date { get; set; } = DateTime.Today;
}