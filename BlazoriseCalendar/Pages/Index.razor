@page "/"

<Container Flex="Flex.JustifyContent.Center.AlignItems.Center" Class="vw-100 vh-100">
    <Calendar @bind-SelectedDate="SelectedDate" SelectionMode="CalendarSelectionMode.Multiple" />
</Container>

@code {
    public DateTime? SelectedDate { get; set; } = DateTime.Today;
}